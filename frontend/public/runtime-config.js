/*
 * Public runtime configuration. This file must never contain secrets.
 * Localhost ignores this public origin and continues using the Angular proxy.
 * This file is public configuration and must never contain credentials.
 */
window.__PORTFOLIO_CONFIG__ = {
  apiBaseUrl: 'https://api.danielramonperez.com',
};
